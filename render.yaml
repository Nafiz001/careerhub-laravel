# Render Blueprint for CareerHub (free tier, Docker runtime).
# Docs: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: careerhub
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: APP_NAME
        value: CareerHub
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      # NOTE: APP_URL is set inside AppServiceProvider::fixHttpsBehindProxy()
      # so the running service doesn't depend on this Blueprint env var being
      # synced (which requires a paid Blueprint plan on free-tier Web Services).
      # Generate once with `php artisan key:generate --show` and paste here,
      # or leave unset and the entrypoint will generate one on boot.
      - key: APP_KEY
        sync: false
      - key: DB_CONNECTION
        value: sqlite
      - key: SESSION_DRIVER
        value: database
      - key: CACHE_STORE
        value: database
      - key: LOG_CHANNEL
        value: stderr
